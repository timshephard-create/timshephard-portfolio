# timshephard.co — NIGHT CREW rebuild

## Standing rules

1. **No fictional provenance.** Any number implying real visit history must be backed by the real store. No cut counter, edition number, or archive count anywhere — in any phase — until the provenance store exists and the numbers are literally true.
2. **/handoff/FACTS.md is copy law.** Every reader-facing claim and every generation call must trace to it. Attribution tiers (A direct / B labeled org outcome / C banned) are non-negotiable. Anything unconfirmed is omitted, never guessed — open items live in its PENDING VERIFICATION section.
3. **Rule 3 Type Law:** all reader-facing text is real DOM text, never baked into images.
4. All rebuild work happens on branch `rebuild` — never commit to main; never push without explicit approval of the shown diff.
5. `build.py` is defunct — do not run it.
6. Design spec: /handoff/production-bible.md + grammar-contract.md (normative), type-manifest.json, plane-tag-map.json. Edition Zero is pure static HTML/CSS, self-hosted fonts, no framework, no build step.
7. Preserve the Formspree endpoint (`https://formspree.io/f/xnjgddyr`) and GA4 (`G-82B9C9WGMG`).
